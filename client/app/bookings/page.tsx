import PageShell from "@/components/ui/PageShell";

export default function BookingsPage() {
  return (
    <PageShell
      eyebrow="Reservations"
      title="Bookings"
      description="View booking details, selected stay dates, checkout progress, payment status, and reservation confirmation."
    />
  );
}
